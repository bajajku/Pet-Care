class Pet {

  // Field variables for Pet, right now basic might change later.
  final String id;
  final String name;
  final String breed;
  final DateTime birthDate;

  // Constructor to create Pet
  Pet({
    required this.id,
    required this.name,
    required this.breed,
    required this.birthDate,
  });
}
